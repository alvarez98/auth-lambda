const { auth } = require('./auth')
const { uuid, id } = require('./id')
const { email } = require('./email')
const { password } = require('./password')
const { user } = require('./user')

module.exports = { auth, uuid, email, password, user, id }
